$kind: http-request
method: DELETE
url: '{{baseUrl}}/listings'
order: 4000
headers:
  - key: Authorization
    value: '{{authToken}}'
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "id": "{{listingId}}"
    }
