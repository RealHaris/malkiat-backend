$kind: http-request
method: POST
url: '{{baseUrl}}/api/auth/reset-password'
order: 6000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "newPassword": "newpassword123",
      "token": "{{resetToken}}"
    }
